#ifndef RECORD_H
#define RECORD_H

#include <string>

struct Record {
    std::string lastName;
    std::string firstName;
    std::string middleName;
    std::string birthDate;
};

#endif // RECORD_H
